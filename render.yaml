services:
  - type: web
    name: rentease-ai-full-stack
    env: node
    plan: free
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: NODE_VERSION
        value: 20.0.0
      - key: PORT
        value: 10000
      - key: JWT_SECRET
        generateValue: true
